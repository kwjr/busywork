<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Background -->
  <rect width="512" height="512" rx="96" fill="#0d1f40"/>
  <!-- Q1: Urgent + Important (red, top-left) -->
  <rect x="60" y="60" width="172" height="172" rx="22" fill="#d43b3b"/>
  <!-- Q2: Schedule (green, top-right) -->
  <rect x="280" y="60" width="172" height="172" rx="22" fill="#00b67a"/>
  <!-- Q3: Admin Time (amber, bottom-left) -->
  <rect x="60" y="280" width="172" height="172" rx="22" fill="#d97706"/>
  <!-- Q4: Backlog (slate, bottom-right) -->
  <rect x="280" y="280" width="172" height="172" rx="22" fill="#7a90a8"/>
</svg>
